// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, type Options as Options2, type RequestResult, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { CreateWebhookSubscriptionData, CreateWebhookSubscriptionErrors, CreateWebhookSubscriptionResponses, ExchangeTokenData, ExchangeTokenErrors, ExchangeTokenResponses, FileJoinRequestData, FileJoinRequestErrors, FileJoinRequestResponses, GetAccountData, GetAccountErrors, GetAccountResponses, GetConfigData, GetConfigResponses, GetDiscoveryData, GetDiscoveryResponses, GetHealthData, GetHealthResponses, GetJwksData, GetJwksResponses, GetOpenApiData, GetOpenApiResponses, GetOrgKeysData, GetOrgKeysErrors, GetOrgKeysResponses, GetSecurityTxtData, GetSecurityTxtResponses, GetSessionCheckData, GetSessionCheckErrors, GetSessionCheckResponses, GetSessionData, GetSessionErrors, GetSessionResponses, GetSessionStateData, GetSessionStateErrors, GetSessionStateResponses, GetTokenPermissionsCatalogData, GetTokenPermissionsCatalogErrors, GetTokenPermissionsCatalogResponses, GetUserinfoData, GetUserinfoErrors, GetUserinfoResponses, IntrospectTokenData, IntrospectTokenResponses, ListKnownDevicesData, ListKnownDevicesErrors, ListKnownDevicesResponses, ListOrganizationsData, ListOrganizationsResponses, ListTokensData, ListTokensErrors, ListTokensResponses, ListWebhookDeliveriesData, ListWebhookDeliveriesErrors, ListWebhookDeliveriesResponses, ListWebhookSubscriptionsData, ListWebhookSubscriptionsErrors, ListWebhookSubscriptionsResponses, ManageTokenData, ManageTokenErrors, ManageTokenResponses, MintTokenData, MintTokenErrors, MintTokenResponses, PushAuthorizationRequestData, PushAuthorizationRequestErrors, PushAuthorizationRequestResponses, RevokeToken2Data, RevokeToken2Errors, RevokeToken2Responses, RevokeTokenData, RevokeTokenResponses, RevokeWebhookSubscriptionData, RevokeWebhookSubscriptionErrors, RevokeWebhookSubscriptionResponses, ScimCreateUserData, ScimCreateUserErrors, ScimCreateUserResponses, ScimDeleteUserData, ScimDeleteUserErrors, ScimDeleteUserResponses, ScimGetUserData, ScimGetUserErrors, ScimGetUserResponses, ScimListUsersData, ScimListUsersErrors, ScimListUsersResponses, ScimPatchUserData, ScimPatchUserErrors, ScimPatchUserResponses, UpdateAccountData, UpdateAccountErrors, UpdateAccountResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * The OIDC discovery document
 */
export const getDiscovery = <ThrowOnError extends boolean = false>(options?: Options<GetDiscoveryData, ThrowOnError>): RequestResult<GetDiscoveryResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetDiscoveryResponses, unknown, ThrowOnError>({ url: '/.well-known/openid-configuration', ...options });

/**
 * The OP's public key set
 */
export const getJwks = <ThrowOnError extends boolean = false>(options?: Options<GetJwksData, ThrowOnError>): RequestResult<GetJwksResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetJwksResponses, unknown, ThrowOnError>({ url: '/jwks.json', ...options });

/**
 * An organization's public signing keys
 *
 * The org-account signing keys' public halves — the registry custody stamps ride along. Anonymous, replayable, short max-age.
 */
export const getOrgKeys = <ThrowOnError extends boolean = false>(options: Options<GetOrgKeysData, ThrowOnError>): RequestResult<GetOrgKeysResponses, GetOrgKeysErrors, ThrowOnError> => (options.client ?? client).get<GetOrgKeysResponses, GetOrgKeysErrors, ThrowOnError>({ url: '/op/keys/{file}', ...options });

/**
 * The token endpoint — the grants, incl. the PAT exchange
 *
 * The OIDC token endpoint. The machine grant is the RFC 8693 token exchange: `grant_type=urn:ietf:params:oauth:grant-type:token-exchange` with `subject_token_type=urn:oimlsmart:params:oauth:token-type:pat` and the personal access token as `subject_token` — answering a short-lived, scope-narrowed OP JWT. The token's scopes are re-judged against the holder's LIVE standing at every exchange.
 */
export const exchangeToken = <ThrowOnError extends boolean = false>(options: Options<ExchangeTokenData, ThrowOnError>): RequestResult<ExchangeTokenResponses, ExchangeTokenErrors, ThrowOnError> => (options.client ?? client).post<ExchangeTokenResponses, ExchangeTokenErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/op/token',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * The token-standing read (RFC 7662)
 *
 * Answers active + the claim set for a live OP-issued access token, and { active: false } for everything else. The token classes: the opaque access tokens (the table read), the self-contained machine JWTs (the signature + the named client's live standing), and the RAW personal access tokens (TODO.openapi/03 — the platform's per-request enforcement read: a live PAT answers active with iss/sub/scope/permissions/service_roles/org/cone/pat/token_type=access_token/exp, all of it the LIVE judgment; revoked, expired, or standing-lost reads inactive). Never an error for an unknown token.
 */
export const introspectToken = <ThrowOnError extends boolean = false>(options: Options<IntrospectTokenData, ThrowOnError>): RequestResult<IntrospectTokenResponses, unknown, ThrowOnError> => (options.client ?? client).post<IntrospectTokenResponses, unknown, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{
            key: 'bearerToken',
            scheme: 'bearer',
            type: 'http'
        }, {
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/op/introspect',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * The revocation (RFC 7009)
 */
export const revokeToken = <ThrowOnError extends boolean = false>(options: Options<RevokeTokenData, ThrowOnError>): RequestResult<RevokeTokenResponses, unknown, ThrowOnError> => (options.client ?? client).post<RevokeTokenResponses, unknown, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{
            key: 'bearerToken',
            scheme: 'bearer',
            type: 'http'
        }, {
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/op/revoke',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * The userinfo
 */
export const getUserinfo = <ThrowOnError extends boolean = false>(options?: Options<GetUserinfoData, ThrowOnError>): RequestResult<GetUserinfoResponses, GetUserinfoErrors, ThrowOnError> => (options?.client ?? client).get<GetUserinfoResponses, GetUserinfoErrors, ThrowOnError>({
    security: [{
            key: 'bearerToken',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/op/userinfo',
    ...options
});

/**
 * The vulnerability disclosure pointer (RFC 9116)
 *
 * The disclosure contact (the repository's private security advisories — the real, monitored channel), the expiry, the preferred languages, and the policy pointer. Refreshed at the yearly deploy review.
 */
export const getSecurityTxt = <ThrowOnError extends boolean = false>(options?: Options<GetSecurityTxtData, ThrowOnError>): RequestResult<GetSecurityTxtResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetSecurityTxtResponses, unknown, ThrowOnError>({ url: '/.well-known/security.txt', ...options });

/**
 * The pushed authorization request (RFC 9126)
 *
 * The FAPI-class posture: the authorize parameter set posted to the back channel (the token endpoint's own client authentication — Basic or post), the browser redirect carrying only the request_uri. The pushed parameters REPLACE the query's at the authorize (any other query parameter is ignored, never merged); the request_uri is SINGLE-USE, lives 90 seconds, and is client-bound (a query client_id must match the owner). The redirect wall applies AT PUSH TIME — an unregistered redirect_uri refuses before anything is stored.
 */
export const pushAuthorizationRequest = <ThrowOnError extends boolean = false>(options: Options<PushAuthorizationRequestData, ThrowOnError>): RequestResult<PushAuthorizationRequestResponses, PushAuthorizationRequestErrors, ThrowOnError> => (options.client ?? client).post<PushAuthorizationRequestResponses, PushAuthorizationRequestErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/op/par',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * The session-management poll iframe
 *
 * The OIDC Session Management iframe (discoverable via check_session_iframe). The relying party embeds it, posts `client_id=…&session_state=…` at it, and it answers `unchanged`/`changed` via postMessage — the recomputation runs server-side per poll against the live session; any failure answers changed (fail-closed).
 */
export const getSessionCheck = <ThrowOnError extends boolean = false>(options: Options<GetSessionCheckData, ThrowOnError>): RequestResult<GetSessionCheckResponses, GetSessionCheckErrors, ThrowOnError> => (options.client ?? client).get<GetSessionCheckResponses, GetSessionCheckErrors, ThrowOnError>({ url: '/op/session/check', ...options });

/**
 * The live session_state digest (the poll's recomputation)
 *
 * The session_state for (client_id, origin, the request's live session) — the value the check iframe compares the relying party's against. The authorize answer (authorization code + state) carries the same digest as `session_state`. 401 = the session is gone = the honest changed.
 */
export const getSessionState = <ThrowOnError extends boolean = false>(options: Options<GetSessionStateData, ThrowOnError>): RequestResult<GetSessionStateResponses, GetSessionStateErrors, ThrowOnError> => (options.client ?? client).get<GetSessionStateResponses, GetSessionStateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/op/session/state',
    ...options
});

/**
 * The liveness probe
 */
export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>): RequestResult<GetHealthResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetHealthResponses, unknown, ThrowOnError>({ url: '/api/health', ...options });

/**
 * The instance's public posture (the branding, the provider flags)
 */
export const getConfig = <ThrowOnError extends boolean = false>(options?: Options<GetConfigData, ThrowOnError>): RequestResult<GetConfigResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetConfigResponses, unknown, ThrowOnError>({ url: '/api/config', ...options });

/**
 * This document (the OpenAPI 3.1 specification)
 *
 * The machine-consumable contract this reference renders — the drift-gated source of truth (every documented operation exists on the app; every app route outside the declared exclusions is documented).
 */
export const getOpenApi = <ThrowOnError extends boolean = false>(options?: Options<GetOpenApiData, ThrowOnError>): RequestResult<GetOpenApiResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetOpenApiResponses, unknown, ThrowOnError>({ url: '/api/openapi.json', ...options });

/**
 * The join-flow register (public)
 *
 * The registered participant orgs plus the active OIML member orgs, each with the roles its kind bounds — the join page's selector feed. Public scheme data; edge-cached (5-minute freshness).
 */
export const listOrganizations = <ThrowOnError extends boolean = false>(options?: Options<ListOrganizationsData, ThrowOnError>): RequestResult<ListOrganizationsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListOrganizationsResponses, unknown, ThrowOnError>({ url: '/api/op/organizations', ...options });

/**
 * File the account request (public, rate-bounded)
 *
 * The public submit: name, work email, the org (from the register) + the role asked for — or the not-listed path (a free-text org name, lands with BIML). One PENDING request per email; no account exists before the enrollment ceremony proves the mailbox.
 */
export const fileJoinRequest = <ThrowOnError extends boolean = false>(options: Options<FileJoinRequestData, ThrowOnError>): RequestResult<FileJoinRequestResponses, FileJoinRequestErrors, ThrowOnError> => (options.client ?? client).post<FileJoinRequestResponses, FileJoinRequestErrors, ThrowOnError>({
    url: '/api/op/join-requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The session's user (or 401)
 */
export const getSession = <ThrowOnError extends boolean = false>(options?: Options<GetSessionData, ThrowOnError>): RequestResult<GetSessionResponses, GetSessionErrors, ThrowOnError> => (options?.client ?? client).get<GetSessionResponses, GetSessionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/auth/session',
    ...options
});

/**
 * The account profile
 */
export const getAccount = <ThrowOnError extends boolean = false>(options?: Options<GetAccountData, ThrowOnError>): RequestResult<GetAccountResponses, GetAccountErrors, ThrowOnError> => (options?.client ?? client).get<GetAccountResponses, GetAccountErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account',
    ...options
});

/**
 * Update the profile (the name; the password change)
 */
export const updateAccount = <ThrowOnError extends boolean = false>(options: Options<UpdateAccountData, ThrowOnError>): RequestResult<UpdateAccountResponses, UpdateAccountErrors, ThrowOnError> => (options.client ?? client).post<UpdateAccountResponses, UpdateAccountErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The account's recognized devices (the risk signals)
 *
 * The known-device record (TODO.modern/06): one row per (account, SHA-256(UA + IP)) with the first/last sighting and countries. The IP is MASKED (the IPv4's last octet folds to *; a non-IPv4 address answers null — never a raw identifier). The sign-in audit's newDevice/countryChanged advisories derive from the same record.
 */
export const listKnownDevices = <ThrowOnError extends boolean = false>(options?: Options<ListKnownDevicesData, ThrowOnError>): RequestResult<ListKnownDevicesResponses, ListKnownDevicesErrors, ThrowOnError> => (options?.client ?? client).get<ListKnownDevicesResponses, ListKnownDevicesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/devices',
    ...options
});

/**
 * The provisioning list (RFC 7644 §3.4.2)
 *
 * Pagination (startIndex, count ≤ 200) + the ONE supported filter: `filter=userName eq "<email>"` — anything else refuses `400 scimType=invalid_filter` (never a silent mis-answer). Erased accounts never appear.
 */
export const scimListUsers = <ThrowOnError extends boolean = false>(options?: Options<ScimListUsersData, ThrowOnError>): RequestResult<ScimListUsersResponses, ScimListUsersErrors, ThrowOnError> => (options?.client ?? client).get<ScimListUsersResponses, ScimListUsersErrors, ThrowOnError>({
    security: [{
            key: 'scimBearer',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/scim/v2/Users',
    ...options
});

/**
 * Provision the invited account (RFC 7644 §3.3)
 *
 * userName IS the account's email. The create maps onto the EXISTING account model: the account row (role viewer) + the one-time enrollment setup link (emailed when a provider is configured — best-effort, never blocking; the console's resend stands behind it). A duplicate userName refuses 409.
 */
export const scimCreateUser = <ThrowOnError extends boolean = false>(options: Options<ScimCreateUserData, ThrowOnError>): RequestResult<ScimCreateUserResponses, ScimCreateUserErrors, ThrowOnError> => (options.client ?? client).post<ScimCreateUserResponses, ScimCreateUserErrors, ThrowOnError>({
    security: [{
            key: 'scimBearer',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/scim/v2/Users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deactivate — never the erase (RFC 7644 §3.6)
 *
 * The deprovision: active=false + the sessions die. The account row NEVER erases (the audit + the history keep it; the erase is the console's own sovereign act).
 */
export const scimDeleteUser = <ThrowOnError extends boolean = false>(options: Options<ScimDeleteUserData, ThrowOnError>): RequestResult<ScimDeleteUserResponses, ScimDeleteUserErrors, ThrowOnError> => (options.client ?? client).delete<ScimDeleteUserResponses, ScimDeleteUserErrors, ThrowOnError>({
    security: [{
            key: 'scimBearer',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/scim/v2/Users/{id}',
    ...options
});

/**
 * The projection (RFC 7644 §3.4.1)
 */
export const scimGetUser = <ThrowOnError extends boolean = false>(options: Options<ScimGetUserData, ThrowOnError>): RequestResult<ScimGetUserResponses, ScimGetUserErrors, ThrowOnError> => (options.client ?? client).get<ScimGetUserResponses, ScimGetUserErrors, ThrowOnError>({
    security: [{
            key: 'scimBearer',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/scim/v2/Users/{id}',
    ...options
});

/**
 * The update — the active replace (RFC 7644 §3.5.2)
 *
 * The supported act: `replace active` (pathful or the pathless value form). `active=false` is the HONEST disable — every session of the account dies with it; the row stays. Anything else refuses `400 scimType=invalidPath`.
 */
export const scimPatchUser = <ThrowOnError extends boolean = false>(options: Options<ScimPatchUserData, ThrowOnError>): RequestResult<ScimPatchUserResponses, ScimPatchUserErrors, ThrowOnError> => (options.client ?? client).patch<ScimPatchUserResponses, ScimPatchUserErrors, ThrowOnError>({
    security: [{
            key: 'scimBearer',
            scheme: 'bearer',
            type: 'http'
        }],
    url: '/scim/v2/Users/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The account's LIVE event subscriptions
 *
 * The account's outbound-webhook subscriptions, active only (revoked rows leave the registry; the delivery history stays). The signing secret NEVER answers here — it was shown exactly once, at the mint.
 */
export const listWebhookSubscriptions = <ThrowOnError extends boolean = false>(options?: Options<ListWebhookSubscriptionsData, ThrowOnError>): RequestResult<ListWebhookSubscriptionsResponses, ListWebhookSubscriptionsErrors, ThrowOnError> => (options?.client ?? client).get<ListWebhookSubscriptionsResponses, ListWebhookSubscriptionsErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/webhooks',
    ...options
});

/**
 * Subscribe an endpoint (the secret answers ONCE)
 *
 * The subscribe: an https public endpoint + the event set (a subset of the journal-action whitelist: account.password, account.session_revoked, account.pat_minted, account.pat_revoked, factor.totp_enrolled, factor.passkey_enrolled). The answer carries the SHARED signing secret exactly once — the subscriber verifies every delivery with it. Deliveries ride `Webhook-Signature: t=<ms>,v1=<hmac-sha256(secret, t + "." + body)>` (the Stripe posture; verify with a 300 s replay bound). The delivery is fire-and-forget and never blocks the act.
 */
export const createWebhookSubscription = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookSubscriptionData, ThrowOnError>): RequestResult<CreateWebhookSubscriptionResponses, CreateWebhookSubscriptionErrors, ThrowOnError> => (options.client ?? client).post<CreateWebhookSubscriptionResponses, CreateWebhookSubscriptionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/webhooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The account's delivery log (newest first)
 *
 * The bounded ladder's outcomes: the attempt count, the last HTTP status, delivered or the dead letter. The body is NEVER recorded — only its SHA-256 digest (the support conversation's dedup key).
 */
export const listWebhookDeliveries = <ThrowOnError extends boolean = false>(options?: Options<ListWebhookDeliveriesData, ThrowOnError>): RequestResult<ListWebhookDeliveriesResponses, ListWebhookDeliveriesErrors, ThrowOnError> => (options?.client ?? client).get<ListWebhookDeliveriesResponses, ListWebhookDeliveriesErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/webhooks/deliveries',
    ...options
});

/**
 * Unsubscribe (the owner's guarded deactivation)
 *
 * Deactivates the subscription — the row stays for the delivery history. Owner-guarded: another account's subscription answers 404.
 */
export const revokeWebhookSubscription = <ThrowOnError extends boolean = false>(options: Options<RevokeWebhookSubscriptionData, ThrowOnError>): RequestResult<RevokeWebhookSubscriptionResponses, RevokeWebhookSubscriptionErrors, ThrowOnError> => (options.client ?? client).delete<RevokeWebhookSubscriptionResponses, RevokeWebhookSubscriptionErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/webhooks/{id}',
    ...options
});

/**
 * The registry + the picker's catalog
 */
export const listTokens = <ThrowOnError extends boolean = false>(options?: Options<ListTokensData, ThrowOnError>): RequestResult<ListTokensResponses, ListTokensErrors, ThrowOnError> => (options?.client ?? client).get<ListTokensResponses, ListTokensErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/tokens',
    ...options
});

/**
 * Mint a token (the plaintext answers ONCE)
 *
 * The mint: the name + the scope picker + the expiration. The plaintext secret answers exactly once — this response — and the store holds only its SHA-256. The scopes must be a subset of the holder's standing under the session's org context. The optional permissions set is validated against the TARGET INSTANCES' served permissions catalogs (fail closed — an instance that cannot answer, or an id outside the served catalogs, refuses the mint).
 */
export const mintToken = <ThrowOnError extends boolean = false>(options: Options<MintTokenData, ThrowOnError>): RequestResult<MintTokenResponses, MintTokenErrors, ThrowOnError> => (options.client ?? client).post<MintTokenResponses, MintTokenErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/tokens',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The mint picker's permissions catalog for one scoped service (TODO.openapi/03)
 *
 * The TARGET INSTANCE's served permissions catalog, fetched server-side and projected to sorted arrays (the OP never holds a copy). The instance resolves from the registered client's own redirect-URI origin. The mint dialog renders groups → checkboxes from it. 502 = the instance cannot answer (the mint will refuse too — fail closed).
 */
export const getTokenPermissionsCatalog = <ThrowOnError extends boolean = false>(options: Options<GetTokenPermissionsCatalogData, ThrowOnError>): RequestResult<GetTokenPermissionsCatalogResponses, GetTokenPermissionsCatalogErrors, ThrowOnError> => (options.client ?? client).get<GetTokenPermissionsCatalogResponses, GetTokenPermissionsCatalogErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/tokens/catalog',
    ...options
});

/**
 * Revoke the token (the row stays for the audit)
 */
export const revokeToken2 = <ThrowOnError extends boolean = false>(options: Options<RevokeToken2Data, ThrowOnError>): RequestResult<RevokeToken2Responses, RevokeToken2Errors, ThrowOnError> => (options.client ?? client).delete<RevokeToken2Responses, RevokeToken2Errors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/tokens/{id}',
    ...options
});

/**
 * The management act — rename, edit the scopes, edit the permissions (issue #115 + TODO.openapi/03)
 *
 * The managed-token act. The rename is presentation-only (from→to on the audit). The scope edit is the complete replacement set, the mint's validation exactly — safe both ways because the exchange re-judges standing on every use. Widening is audited (and mailed) distinctly. The permissions edit replaces the pinned catalog-id set, validated against the scoped instances' served catalogs (fail closed); ADDING a permission is the widening-sensitive act (a stale session refuses with fresh_auth_required). A revoked or expired token refuses edits.
 */
export const manageToken = <ThrowOnError extends boolean = false>(options: Options<ManageTokenData, ThrowOnError>): RequestResult<ManageTokenResponses, ManageTokenErrors, ThrowOnError> => (options.client ?? client).patch<ManageTokenResponses, ManageTokenErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'oiml-session',
            type: 'apiKey'
        }],
    url: '/api/op/account/tokens/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
