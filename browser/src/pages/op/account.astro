---
// The account console, native on this host (the wave-02a frontend: the
// console pages never redirect into the platform app).
import IdShell from '../../layouts/IdShell.astro'
export const prerender = true
---
<IdShell title="Your account" page="account" />
