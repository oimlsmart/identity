---
// The pre-02a path redirects to the identity-native sign-in (the host's
// root IS the sign-in page). The query string does not carry across a
// static redirect — a stale in-flight authorize chain simply restarts.
export const prerender = true
return Astro.redirect('/')
---
