---
// The OP's one account surface is the account console: the kernel's
// roleHome() answers /app for a console-less role, so /app redirects
// here (the same posture as /op/account).
export const prerender = true
return Astro.redirect('/app/account')
---
