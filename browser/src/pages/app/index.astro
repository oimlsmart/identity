---
// The kernel's roleHome() answers /app for a console-less role; on this
// host the signed-in landing is the SSO home (the launcher).
export const prerender = true
return Astro.redirect('/op/home')
---
