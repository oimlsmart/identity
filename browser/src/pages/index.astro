---
// The identity host's root IS the sign-in page (the wave-02a
// identity-native frontend: an identity service is not a platform app
// with extra routes) — the OP password form, the linked-method buttons,
// the one-line footer + the status link in the shell's bare posture.
import IdShell from '../layouts/IdShell.astro'
export const prerender = true
---
<IdShell title="Sign in" page="public/login" bare />
