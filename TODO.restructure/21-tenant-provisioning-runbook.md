# TODO.restructure/21 — the tenant provisioning runbook

**Priority:** P1 · **Status:** COMPLETE

`docs/deployment/identity-whitelabel.md` — the seven acts (the flavor
profile, the brand assets, issuer + signing key, the bootstrap
administrator seed, the tenant's RP client seed, the deploy posture
pointer to identity-self-host.md, the optional central-OP federation),
plus the "what a whitelabel instance is NOT" boundary (its own
registry, its declared console, never the central data).
